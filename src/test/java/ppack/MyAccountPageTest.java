package ppack;

import org.testng.Assert;
import org.testng.annotations.Test;

import objectrepository.MyAccountPage;

public class MyAccountPageTest extends BaseTest {

    // @Test
    // public void confirmAccountPage() throws InterruptedException {
    // MyAccountPage page = new MyAccountPage();
    // Thread.sleep(4000);
    // driver = page.getDriver();
    // Assert.assertTrue(page.accountBreadcrumb().isDisplayed());
    // }
}